import React from "react";

import Uploader from "../components/Uploader";

function UploadAudio({ history }) {
    return (
        <>
            <Uploader history={history} />
        </>
    );
}

export default UploadAudio;
